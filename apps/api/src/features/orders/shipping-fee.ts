// Flat fee for the whole order when it holds at least one physical edition.
// A product decision, not deployment configuration, unlike STRIPE_PUBLISHABLE_KEY
// or a port - see the root CLAUDE.md's Configuration rule.
export const SHIPPING_FEE_CENTS = 500
