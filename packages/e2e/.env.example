# URL of the locally running web app (apps/web), e.g. `pnpm dev:web`
E2E_BASE_URL=http://localhost:3000

# URL of the locally running API (apps/api), e.g. `pnpm dev:api`
E2E_API_URL=http://localhost:8080
